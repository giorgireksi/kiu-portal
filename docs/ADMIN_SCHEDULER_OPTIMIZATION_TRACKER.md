# Admin Scheduler Optimization Tracker

Target page: `admin-scheduler.html`
Last updated: `2026-05-18`
Owner: `Codex`
Goal: keep the scheduler visually strong while reducing inline handler debt, preserving admin workflows, and using a real tracker for the next split pass.

## Baseline

| Metric | Value | Evidence |
| --- | --- | --- |
| Entry HTML | `admin-scheduler.html` | `70,133 bytes` after removing the unneeded `assets/js/app/api.js` shell import |
| Page runtime | `assets/js/pages/admin-scheduler.js` | `65,282 bytes` after moving modal ownership, quiz actions, and quick-action/week wiring into the page controller |
| External scripts | `10` | Script-tag inventory after removing the unneeded `assets/js/app/api.js` shell import |
| Inline handlers | `0` raw HTML `onclick=` hits, `0` live-controller `onclick=` hits | Source scan after delegating the active palette/grid/session controller and collapsing the disabled legacy scheduler engines to inert placeholders. |
| Blurred overlays | `20` `backdrop-filter` hits | Route CSS still contains the same surface families, but they now read through reduced `--sch-glass-blur-*` variables instead of the old stronger per-surface blur multipliers |
| Heavy shadows | `15` `box-shadow` hits | Route CSS still contains the same surface families, but repeated panel/card/modal shadows now flow through lower `--sch-*-shadow` variables |
| Shared verification | `10/10` targeted assertions passed | `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` |
| Browser artifacts | `2` startup interaction summaries | `artifacts/admin-scheduler-efficient-desktop-summary.json`, `artifacts/admin-scheduler-mobile-summary.json` |

## Current Findings

1. `admin-scheduler.html` still boots the shared admin shell, but the live `assets/js/pages/admin-scheduler.js` controller now owns the quick-action buttons, week navigation, create-session modal, and professor quiz modal instead of relying on the deleted dead inline interaction block.
2. The active palette cards, slot cells, event cards, stats buttons, delete buttons, and quiz-builder inputs route through delegated `data-*` actions instead of generated inline handlers.
3. Both hidden scheduler overlays now stay inside `<template>` nodes until first open, so the page no longer mounts the create-session or professor-quiz workspaces at startup.
4. The repeated palette cards, event cards, panels, and modal surfaces now preserve the same gradients, borders, and light-mode identity while using lower route-scoped blur/shadow variables.
5. The unproven `messenger.js`, `assets/js/features/ui.js`, and `assets/js/app/api.js` shell imports are all gone, and blocking probes now show `assets/js/app/app.js` plus `assets/js/features/navigation.js` still own required scheduler shell behavior.
6. Real weak-device/browser evidence now exists for startup, week render, slot-open, and edit-modal latency on both efficient desktop and mobile.
7. No scheduler-specific cleanup tasks remain open; any future work should reuse the current probe instead of reopening import churn without proof.
8. The scheduler shell source no longer ships mojibake-heavy comment/header blocks; the focused recovery test now guards that cleaned source baseline.
9. The scheduler shell no longer ships UTF-8 BOM bytes, malformed `datalist` options, XHTML-style hidden inputs, or raw `&` source text in the create-session CTA.
10. The first root-entry markup hardening batch is now landed on `admin-scheduler.html`: the hidden nav landmarks have unique labels, the four wrapped filter labels no longer ship redundant `for` attributes, the visible mobile-sheet buttons now declare `type="button"` and use `<span class="mob-sheet-icon">`, and focused `html-validate` dropped the page from `41` to `25` issues, leaving primarily inline-style debt in the shell templates.
11. The second root-entry markup batch completed the entry-page validator cleanup for `admin-scheduler.html`: the subject-search accessibility label, professor quiz modal template, create-session modal template, readonly faculty field, and mobile action-sheet icon gradients now all use scheduler-owned classes, and focused `html-validate` reports `0` issues on the page.

## AI Update Rules

1. Update this file in the same turn as every change to `admin-scheduler.html` or `assets/js/pages/admin-scheduler.js`.
2. Update `% left` immediately for every touched task.
3. Add one `Change Log` row per edit batch with files touched, task IDs touched, and verification used.
4. Do not mark a task done without direct code inspection or targeted test output.
5. Preserve scheduler layout, week navigation, and admin workflow unless a task explicitly changes them.
6. If a task is blocked by dependency coupling, mark it `Blocked` and explain why.
7. `% left` scale:
   `0%` = done
   `1-15%` = almost done
   `16-60%` = partly done
   `61-99%` = mostly not done
   `100%` = untouched

## Task Board

| ID | Status | % left | Task | Notes |
| --- | --- | ---: | --- | --- |
| ASCH-01 | Done | 0% | Replace all inline grid, palette, and modal `onclick` handlers with delegated listeners | The live controller now has `0` `onclick=` hits, the active quiz-builder shell uses delegated `data-*` hooks, and the disabled legacy scheduler engines are reduced to inert placeholders with no raw `onclick=` debt left in the HTML. |
| ASCH-02 | Done | 0% | Move schedule-cell and event-card markup generation out of raw string concatenation and into page-owned render helpers | `renderPalette()` and `renderGrid()` now build palette cards, day headers, time slots, slot backgrounds, event cards, and info banners through dedicated DOM helper functions plus `replaceChildren(...)`. |
| ASCH-03 | Done | 0% | Reduce overlay and card blur intensity while preserving the same visual theme on desktop and mobile | Route-scoped `--sch-*-shadow` and `--sch-glass-blur-*` variables now lower the repeated panel/card/modal blur and shadow cost while keeping the same gradients, borders, and light-mode visual language. |
| ASCH-04 | Done | 0% | Stop building invisible scheduler content up front; mount only the active faculty, semester, and week view | `admin-scheduler.html` now keeps both hidden scheduler overlays inside templates, `assets/js/pages/admin-scheduler.js` mounts them through `ensureMountedTemplate(...)`, and the live grid/palette continue to render only the current faculty/semester/week state. |
| ASCH-05 | Done | 0% | Audit whether this page still needs the full shared shell stack or can run on a thinner admin shell | The current `10`-script shell is now the proven floor: blocking `assets/js/app/app.js` breaks auth/session constants (`USER_ROLES`, `SCHEDULER_WEEK_STORAGE_KEY`) and route readiness, while blocking `assets/js/features/navigation.js` leaves the mobile route nav without `window.navigate`; the removed `messenger.js`, `ui.js`, and `api.js` imports were the only safe shell cuts. |
| ASCH-06 | Done | 0% | Create a dedicated scheduler tracker | This file is the dedicated tracker. |
| ASCH-07 | Done | 0% | Capture weak-laptop and mobile checks for week render, slot open, and edit modal open latency | Efficient-desktop and mobile artifacts now capture first-ready, week-render, slot-open, and edit-modal timings through a seeded visible event-card path, all with zero runtime errors. |
| ASCH-08 | Done | 0% | Remove mojibake-heavy comment/header noise from the scheduler shell source | `admin-scheduler.html` and the shared `assets/css/layout.css` / `assets/css/social-rebuild.css` comment blocks are now source-clean, and `test/admin-scheduler-recovery.test.js` guards the old mojibake marker from returning in the shell HTML. |
| ASCH-09 | Done | 0% | Remove the remaining validator-confirmed structural shell faults in `admin-scheduler.html` | The entry HTML is now saved without BOM, the hidden quiz inputs use standard HTML void-tag syntax, the two `datalist` blocks use explicit sibling `<option>` markup, and the create-session CTA now encodes `&amp;` in source. |

## Change Log

| Date | Files | Tasks | Evidence |
| --- | --- | --- | --- |
| `2026-05-17` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `test/admin-scheduler-navigation.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md` | `ASCH-09` | Removed the UTF-8 BOM from `admin-scheduler.html`, normalized the three hidden quiz inputs away from XHTML-style self-closing tags, expanded the scheduler `datalist` blocks into explicit sibling `<option>` elements, and encoded `Create Session &amp; Deploy` in source; `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` passed `2/2` files and `10/10` tests; focused `npx -y html-validate admin-scheduler.html` no longer reports the earlier `no-utf8-bom`, `void-style`, `no-raw-characters`, or `no-implicit-close` failures; and raw buffer verification now confirms the file ships without BOM bytes. |
| `2026-05-18` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `test/admin-scheduler-navigation.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/FULL_SITE_SYNTAX_BUG_SECURITY_AUDIT.md` | `ASCH-09` | Added unique labels to the hidden nav stubs, removed the redundant `for` attributes from the wrapped filter labels, and normalized the mobile-sheet buttons to explicit `type="button"` plus `<span class="mob-sheet-icon">` wrappers; focused `html-validate` dropped `admin-scheduler.html` from `41` to `25` issues; and `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` stayed green at `2/2` files and `10/10` tests. |
| `2026-05-18` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `test/admin-scheduler-navigation.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/FULL_SITE_SYNTAX_BUG_SECURITY_AUDIT.md` | `ASCH-09` | Replaced the remaining subject-search label, professor-quiz modal, create-session modal, readonly faculty field, and mobile action-sheet inline styles with scheduler-owned classes inside the existing route style block; focused `html-validate` dropped `admin-scheduler.html` from `25` to `0`; and `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` stayed green at `2/2` files and `10/10` tests. |
| `2026-05-17` | `admin-scheduler.html`, `assets/css/layout.css`, `assets/css/social-rebuild.css`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md` | `ASCH-08` | Removed the mojibake-heavy comment/header blocks from the scheduler shell source and the shared CSS headers referenced by this route; `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` passed `5/5`; and direct source scans now show `0` remaining mojibake markers in `admin-scheduler.html`. |
| `2026-05-14` | `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md` | `ASCH-06` | Baseline from `admin-scheduler.html`, `assets/js/pages/admin-scheduler.js`, and existing scheduler recovery/navigation tests. |
| `2026-05-14` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js` | `ASCH-01` | Delegated the quiz modal close handlers and reduced the shell inline-handler count to `27`. |
| `2026-05-14` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js` | `ASCH-01` | Removed the quiz/scheduler modal close `onclick` handlers; `admin-scheduler.html` now reports `27` inline handlers. |
| `2026-05-15` | `assets/js/pages/admin-scheduler.js`, `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-01` | `node --check assets/js/pages/admin-scheduler.js` passed; `npx vitest run test/admin-scheduler-recovery.test.js` passed all `5` assertions; `assets/js/pages/admin-scheduler.js` now reports `0` `onclick=` hits with delegated `data-scheduler-subject-id`, `data-scheduler-slot-day`, and `data-scheduler-session-action` hooks; `admin-scheduler.html` now reports `0` `onchange=` hits and `14` raw `onclick=` hits, all remaining inside the two disabled legacy script blocks. |
| `2026-05-16` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-01` | Collapsed the two disabled legacy scheduler engines into inert audit placeholders, cutting `admin-scheduler.html` to `79,024` bytes and removing the last `14` raw `onclick=` hits; `npx vitest run test/admin-scheduler-recovery.test.js` stayed green at `5/5`, and direct source scans now show `0` `onclick=` plus `0` `onchange=` hits in the HTML shell. |
| `2026-05-16` | `admin-scheduler.html`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-05` | Removed the unproven `assets/js/shared/messenger.js` shell import from `admin-scheduler.html`; `npx vitest run test/admin-scheduler-recovery.test.js` stayed green at `5/5`; direct source scans now show `12` external scripts, no `messenger.js` import, and the same delegated live controller path. |
| `2026-05-16` | `assets/js/pages/admin-scheduler.js`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-02` | Reworked `renderPalette()` and `renderGrid()` to use dedicated DOM helpers for palette cards, day headers, time slots, slot backgrounds, event cards, and info banners; `node --check assets/js/pages/admin-scheduler.js` passed; `npx vitest run test/admin-scheduler-recovery.test.js` stayed green at `5/5`; direct source scans now show `buildSchedulerPaletteCard(...)`, `buildSchedulerSlotBackground(...)`, `buildSchedulerEventCard(...)`, `list.replaceChildren(fragment)`, and `container.replaceChildren(fragment)` with no remaining live `html += '<div class=\"sch-slot-bg\"'` or `html += '<div class=\"sch-event\"'` paths. |
| `2026-05-16` | `admin-scheduler.html`, `assets/js/pages/admin-scheduler.js`, `test/admin-scheduler-recovery.test.js`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-03`, `ASCH-04`, `ASCH-05` | Moved the create-session and professor-quiz overlays into `<template>` nodes, deleted the dead inline scheduler interaction block, moved quick-action/week wiring plus quiz ownership into `assets/js/pages/admin-scheduler.js`, and lowered repeated panel/card/modal blur/shadow intensity through route-scoped variables; `node --check assets/js/pages/admin-scheduler.js` passed; `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` passed at `9/9`; direct source scans now show `2` template nodes, `0` `onclick=` hits, `0` `onchange=` hits, `schedulerShellActionBound`, `schedulerWeekActionBound`, `ensureMountedTemplate(...)`, `function openProfQuizModal(...)`, `70,296` HTML bytes, and `65,282` page-runtime bytes. |
| `2026-05-16` | `tools/capture_admin_scheduler_summary.mjs`, `artifacts/admin-scheduler-efficient-desktop-summary.json`, `artifacts/admin-scheduler-mobile-summary.json`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-07` | Added a Playwright scheduler probe and captured efficient-desktop plus mobile route summaries; `node --check tools/capture_admin_scheduler_summary.mjs` passed; `artifacts/admin-scheduler-efficient-desktop-summary.json` now records `firstReadyMs: 2060`, `weekRenderMs: 54`, `slotOpenMs: 191`, `editModalOpenMs: 59`, `performanceTier: efficient`, `eventCount: 1`, and zero errors; `artifacts/admin-scheduler-mobile-summary.json` now records `firstReadyMs: 688`, `weekRenderMs: 23`, `slotOpenMs: 24`, `editModalOpenMs: 16`, `mobileNavVisible: true`, `eventCount: 1`, and zero errors. |
| `2026-05-16` | `admin-scheduler.html`, `artifacts/admin-scheduler-efficient-desktop-summary.json`, `artifacts/admin-scheduler-mobile-summary.json`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-05` | Removed the unneeded `assets/js/features/ui.js` shell import from `admin-scheduler.html`; `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` stayed green at `9/9`; direct source scans now show `11` external scripts, no `ui.js` import, and the refreshed scheduler artifacts still pass with zero errors (`firstReadyMs: 2202`, `weekRenderMs: 65`, `slotOpenMs: 95`, `editModalOpenMs: 89` on efficient desktop; `firstReadyMs: 763`, `weekRenderMs: 25`, `slotOpenMs: 23`, `editModalOpenMs: 20` on mobile). |
| `2026-05-16` | `admin-scheduler.html`, `artifacts/admin-scheduler-efficient-desktop-summary.json`, `artifacts/admin-scheduler-mobile-summary.json`, `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-05` | Removed the unneeded `assets/js/app/api.js` shell import from `admin-scheduler.html`; `npx vitest run test/admin-scheduler-recovery.test.js test/admin-scheduler-navigation.test.js` stayed green at `9/9`; direct source scans now show `10` external scripts, no `api.js` import, and the refreshed scheduler artifacts still pass with zero errors (`firstReadyMs: 2470`, `weekRenderMs: 146`, `slotOpenMs: 385`, `editModalOpenMs: 68` on efficient desktop; `firstReadyMs: 795`, `weekRenderMs: 21`, `slotOpenMs: 18`, `editModalOpenMs: 11` on mobile). |
| `2026-05-16` | `docs/ADMIN_SCHEDULER_OPTIMIZATION_TRACKER.md`, `docs/ALL_PAGES_CLEANUP_MASTER_AUDIT.md` | `ASCH-05` | Closed the remaining shared-shell audit after import-floor probes: aborting `assets/js/app/app.js` prevents route readiness (`USER_ROLES` / `SCHEDULER_WEEK_STORAGE_KEY` missing), and aborting `assets/js/features/navigation.js` leaves the mobile dynamic nav without `window.navigate`, so the current `10`-script shell is the minimum safe set for this standalone page. |

## Next Safe Pass

1. Reuse the scheduler probe whenever week/grid/modal behavior changes again.
2. Treat the current `10`-script shell as the floor unless a future pass produces new import-blocking evidence.
3. Keep feature work on `assets/js/pages/admin-scheduler.js`, not on disabled legacy placeholders.
